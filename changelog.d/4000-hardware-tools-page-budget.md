### Docs: the hardware tools page keeps the option domains, not the narrative

`docs/hardware/tools.md` was 3,751 words - the option domains a caller needs,
buried under paragraphs retelling how each bound was found. The generated tool
reference already carries every parameter, default and refusal, so the page now
keeps the tool index, the domain tables, the session retention and `stop`
verdict tables, calibration and the examples, at 1,489 words. It is no longer
exempted from the 1,500-word page budget.
