### Fixed: the GR00T page's `data_configs` block names every shipped config. The block headed "27 data_configs" on docs/policies/groot.md listed 24 names, so `bimanual_panda_hand` and `oxe_droid_relative_eef_relative_joint` could not be found from the page even though `data_configs.json` ships both. They are named now, and a test keyed on the live catalog fails when the block falls behind it again.
