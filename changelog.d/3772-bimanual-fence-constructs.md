### Fixed: the bimanual docs fence only names robots that construct on a clean install. `docs/robots/bimanual.md` listed `Robot("bi_openarm")` and `Robot("trossen_wxai")` beside the sim-only Aloha line, but `bi_openarm` is registered for real hardware only and `trossen_wxai`'s asset is never auto-downloaded, so both lines raised before a reader reached the hardware example. The fence now shows `bi_openarm` as a `mode="real"` rig with one follower config per arm, like `bi_so_follower`, and names the `trossen_wxai/trossen_ai_bimanual.xml` file to place under `STRANDS_ASSETS_DIR` before the WX-AI sim line runs.
