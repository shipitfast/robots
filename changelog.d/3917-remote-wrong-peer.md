### Fixed: `RemotePolicy` dialled at a port that is listening but does not speak WebSocket (an HTTP server, a ZMQ sidecar, a socket that closes on accept) raised a bare `websockets.exceptions.InvalidMessage: did not receive a valid HTTP response`, naming neither the endpoint nor what to check, from a connect documented to raise `ConnectionError`. It now raises the documented `ConnectionError` naming the URI, that the peer accepted the connection but did not complete the WebSocket handshake, and the check-the-port remedy, kept apart from the "Start one first" hint an absent server still gets.
