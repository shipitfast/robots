### Fixed: `lerobot_teleoperate`'s `action` parameter description named five verbs while the tool dispatched six. The tool description's Actions block already named `dagger`, so the two halves of one spec disagreed about which verbs exist, and the enum an agent reads to fill that argument was the half that omitted it. The parameter description now lists every dispatched verb, and a test keeps the two in step.
