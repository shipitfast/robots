### Fixed:
- A MuJoCo `Simulation` the caller never released is now torn down at process exit. Teardown ran only from `__del__`, which at interpreter shutdown reaches a path whose module globals CPython has already set to `None`: the first step raised `AttributeError`, the finalizer logged `Cleanup error during __del__: 'NoneType' object has no attribute 'suppress'`, and nothing was released - the ROS 2 bridge node stayed up, teleoperated devices stayed connected, the simulation stayed advertised to the fleet as a live peer, and policy workers were never joined before the world they were stepping was freed. Release now also runs from an `atexit` hook, while the import system is intact.
